# Governance

`crux-console` is maintained under the `cruxctl` GitHub organization.

Design issues are required for navigation changes, role-sensitive workflows, approval UX, trace redaction, and major visual system changes.
Releases will follow semantic versioning after the first implementation release.
