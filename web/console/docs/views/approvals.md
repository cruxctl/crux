# Approvals View

The approvals view should show pending, approved, denied, and expired approval requests.

Each request should expose:

- requesting agent,
- tool or model action,
- policy that required approval,
- risk summary,
- requester context,
- decision history.
