# Executions View

The executions view should show live and historical jobs.

Each execution needs:

- agent,
- status,
- start and end time,
- cost,
- fallback or handoff status,
- trace link,
- error summary when failed.
