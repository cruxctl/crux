# MCP Registry View

The MCP registry view should show known MCP servers, versions, trust status, schema drift, tool lists, install counts where available, and policy state.

Operators should be able to inspect, allowlist, block, quarantine, and review server history from this view.
