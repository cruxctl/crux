# Development

No implementation toolchain has landed yet.

When implementation begins:

- keep domain features isolated under `src/features`,
- prefer shared typed API clients,
- verify views at desktop and mobile widths,
- avoid decorative dashboards that hide operational detail,
- update view docs with every workflow change.
